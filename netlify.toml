[build]
  command = "npm run build"
  publish = ".next"

# Netlify's official Next.js runtime — supports App Router, API routes,
# next/image and server components out of the box.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
